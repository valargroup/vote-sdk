# Shielded-Vote chain REST API URL.
# Used as the Vite dev-server proxy target. In production the UI defaults to
# same-origin requests (the UI is served in-process by svoted), so this is
# only needed for local development or when the API is on a different host.
# Default (dev): http://localhost:1318
VITE_CHAIN_URL=http://localhost:1318

# Lightwalletd gRPC-web endpoint for Zcash mainnet.
# Used as the default RPC endpoint for chain height / note scanning.
# Default: https://us.zec.stardust.rest (first entry in the built-in endpoint list)
VITE_LIGHTWALLETD_RPC=https://us.zec.stardust.rest

# Vote-manager private key (hex) for the admin UI's dev auto-connect. Must be
# one of the keys in the chain's VM_PRIVKEYS set (by convention, the first).
# Copy one entry from vote-sdk/.env's VM_PRIVKEYS into this variable manually.
# Leave empty to disable auto-connect (you can still connect via Keplr etc.).
VITE_VM_PRIVKEY=
