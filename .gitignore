# Go binary
/svoted
/create-val-tx

# Go build cache
.cache/

# Rust build artifacts
circuits/target/
e2e-tests/target/

# Environment (secrets live here — never commit)
.env
.env.*
!.env.example

# Docker testnet (auto-generated)
docker/docker-compose.yml

# Admin UI (build artifacts + local env)
ui/node_modules/
ui/dist/

# macOS
.DS_Store

# IDE
.idea/
.vscode/
*.swp
