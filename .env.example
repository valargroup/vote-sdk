# vote-sdk local development environment
# Copy to .env and fill in values:  cp .env.example .env

# Vote-manager private keys (required).
# Comma-separated list of 64-character hex secp256k1 private keys. Every
# derived address becomes a vote manager at genesis (any-of-N); the stake pool is
# split evenly across the set. For a single-vote-manager chain, provide exactly one
# key. Generate one with:  openssl rand -hex 32
VM_PRIVKEYS=
